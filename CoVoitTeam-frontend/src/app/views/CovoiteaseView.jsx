import React, { useState } from "react";

const CovoiteaseView = () => {
  const [isModeAddVehicule, setIsModeAddVehicule] = useState(false);
  let userId = "208";

  return <div>Covoiturage du quotidien.</div>;
};

export default CovoiteaseView;
